# September 2026 release

Backend health releaseVersion: 20260905-review-functional-update.

Public browsing no longer triggers sign-in. Private operations still require a session.
The Mini Program source is included in miniprogram/ for version tracking, but is not served by the Node backend.
Upload miniprogram/ separately through WeChat DevTools with the existing AppID. Deploying Render does not publish Mini Program frontend code.

Features: public school/program catalog; reviewed administrator posts; free public courses; contact-request bookings; private course feedback; student original-file retrieval; truthful event-statistic boundaries; structured shared CV experiences; motivation PDF page limits; embedded fonts.

New MP_OFFICIAL_ACCOUNT_USERNAME may contain the associated official account original gh_ ID. Leave empty until supplied. It is not an AppSecret.

Preserve all current production environment variables and the existing private COS bucket.
Do not enable payment or an independent database for this release.
Do not replace production data/*.jsonl or private upload directories with test fixtures.
The bundled demo video remains available in assets/course-videos; it is not itself a COS upload.
Other existing course records are not silently changed from private to public. A free course must explicitly be marked free and published.

The local document engine is a factual structuring tool, not a full general-purpose translator.
Untranslated input is visibly flagged and must be translated/reviewed before application use.
CV source questionnaire reference data was not copied to code or fixtures.

WeChat release owner must still update the hosted privacy guide contact, add the service category requested by review, and complete any required qualifications. In-app AI labels do not replace those platform requirements.

Tests: npm test. Full device coverage and production concurrency are not certified by these tests.
